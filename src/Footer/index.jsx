import React, { Component } from 'react';
import t from "prop-types";
import { render } from "react-dom";

class Footer extends Component {
  render() {
    return <div> Show Selected </div>;
  }
}
export default Footer;
